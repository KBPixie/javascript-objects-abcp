var playlist = {
  artistName: "songTitles"
};

function updatePlaylist(playlist, artistName, songTitle){
  playlist[Beatles] = michelle;
  return playlist;
}

function removeFromPlaylist(playlist, artistName, songTitle){
  
}